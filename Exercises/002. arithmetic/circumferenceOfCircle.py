# we want to calcualte the circumference of a circle ( 2 π r )
import math

pi = math.pi

radius = float(input("What is the radius of the circle ? "))

circumference = 2 * pi * radius

print(f"The circumference of your circle is {circumference}")